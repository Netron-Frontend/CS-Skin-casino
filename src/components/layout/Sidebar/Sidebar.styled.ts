import styled from "styled-components";

export const SidebarWrapper = styled.div`
		width: 193px;
		height: 100vh;
		display: flex;
		flex-direction: column;
		align-items: center;
		background: #04081F;
		color: #D8D8D8;
		gap: 26px;
		position: sticky;
    left: 0;
    z-index: 1;
		margin-top: 1px;
		padding-top: 42px;
    filter: drop-shadow(5px 4px 5px #3D3D3D);
`